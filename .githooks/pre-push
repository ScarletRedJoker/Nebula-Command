#!/bin/bash
# Git pre-push hook - validates code before push

echo "🔒 Running pre-push validation..."

if bash scripts/validate-for-ubuntu.sh; then
    echo "✅ Validation passed - proceeding with push"
    exit 0
else
    echo "❌ Validation failed - push blocked"
    echo "Fix errors and try again, or use 'git push --no-verify' to bypass"
    exit 1
fi
