# Nebula Command Dashboard - Local Host Environment Variables
# Copy this file to .env and fill in the values
# Run: cp .env.example .env

# ============================================
# REQUIRED: Plex Media Server
# ============================================
# Get token from: https://www.plex.tv/claim/
PLEX_TOKEN=your_plex_claim_token
PLEX_CLAIM=claim-xxxxxxxxxxxxx

# ============================================
# REQUIRED: MinIO Object Storage
# ============================================
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=GENERATE_ME_SECURE_PASSWORD

# ============================================
# REQUIRED: Home Assistant
# ============================================
# Generate long-lived token in Home Assistant:
# Profile -> Long-Lived Access Tokens -> Create Token
HOME_ASSISTANT_TOKEN=your_home_assistant_long_lived_token

# ============================================
# Cloudflare Tunnel (Recommended)
# ============================================
# Exposes local services (Plex, Home Assistant) via Cloudflare
# without port forwarding or firewall configuration.
#
# Setup: See docs/deploy/CLOUDFLARE_TUNNEL.md
#
# Option 1: Token-based (simpler)
# Get token: cloudflared tunnel token homelab-tunnel
CLOUDFLARE_TUNNEL_TOKEN=

# Option 2: Credentials file (config/cloudflared/credentials.json)
# Create tunnel: cloudflared tunnel create homelab-tunnel
CLOUDFLARE_TUNNEL_ID=

# ============================================
# Storage Monitoring
# ============================================
# Start with: docker compose --profile monitoring up -d

# Enable ZFS pool monitoring (if using ZFS)
ZFS_ENABLED=false

# Discord webhook for storage alerts (recommended)
# Create at: Discord Server Settings > Integrations > Webhooks
STORAGE_ALERT_DISCORD_WEBHOOK=

# Email for storage alerts (optional)
STORAGE_ALERT_EMAIL=

# Health check interval in seconds (default: 1 hour)
STORAGE_CHECK_INTERVAL=3600

# SMART thresholds before alerting
SMART_THRESHOLD_REALLOCATED=5
SMART_THRESHOLD_PENDING=1

# ============================================
# WireGuard VPN Configuration
# ============================================
# This host's WireGuard IP (should match Linode peer config)
WIREGUARD_IP=10.200.0.2/24

# Linode server's WireGuard IP
WIREGUARD_PEER_IP=10.200.0.1

# Linode server's public IP
LINODE_PUBLIC_IP=your_linode_ip

# ============================================
# OPTIONAL: Sunshine GameStream
# ============================================
# Start with: docker compose --profile gamestream up -d
SUNSHINE_PASS=your_sunshine_web_ui_password

# Windows VM IP (KVM/libvirt)
WINDOWS_VM_IP=192.168.122.250

# ============================================
# OPTIONAL: VNC Desktop
# ============================================
# Start with: docker compose --profile vnc up -d
VNC_PASSWORD=GENERATE_ME_8_CHAR_PASSWORD

# ============================================
# OPTIONAL: NAS Access
# ============================================
NAS_HOST=192.168.1.x
NAS_USER=admin
NAS_PASSWORD=your_nas_password

# ============================================
# Timezone
# ============================================
TZ=America/New_York
