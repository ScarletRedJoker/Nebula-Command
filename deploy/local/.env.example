# ═══════════════════════════════════════════════════════════════
#  Nebula Command - Local Ubuntu Environment Configuration
#  
#  QUICK SETUP:
#    ./deploy.sh           # Auto-generates secrets & deploys
#    ./deploy.sh setup     # Interactive environment setup only
#    ./deploy.sh check     # Verify configuration
#    ./deploy.sh dns-sync  # Sync Cloudflare DNS records
#    ./deploy.sh install   # Install systemd service for boot start
# ═══════════════════════════════════════════════════════════════

# ━━━ DOMAIN & DNS (REQUIRED for public access) ━━━
DOMAIN=evindrake.net
ADMIN_EMAIL=admin@evindrake.net
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here

# ━━━ AUTO-GENERATED (deploy.sh handles these) ━━━
# These are generated automatically on first deploy if not set:
# POSTGRES_PASSWORD, AUTHELIA_JWT_SECRET, AUTHELIA_SESSION_SECRET, AUTHELIA_STORAGE_KEY

# ━━━ VPN PROVIDER (Required for private torrents) ━━━
# Get from Mullvad account or your VPN provider
VPN_PROVIDER=mullvad
MULLVAD_PRIVATE_KEY=your_wireguard_private_key_here
MULLVAD_ADDRESS=10.x.x.x/32
VPN_COUNTRY=Switzerland

# ━━━ LOCAL USER ━━━
LOCAL_USER=evin
SSH_USER=evin

# ━━━ TIMEZONE ━━━
TZ=America/New_York

# ━━━ PLEX MEDIA SERVER ━━━
# Get token from: https://www.plex.tv/claim/
PLEX_TOKEN=
PLEX_CLAIM=claim-xxxxxxxxxxxxx

# ━━━ MINIO OBJECT STORAGE ━━━
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=

# ━━━ HOME ASSISTANT ━━━
# Generate: Profile -> Long-Lived Access Tokens -> Create Token
HOME_ASSISTANT_TOKEN=

# ━━━ VNC REMOTE DESKTOP ━━━
VNC_PORT=5900
VNC_PASSWORD=

# ━━━ TAILSCALE MESH NETWORK ━━━
TAILSCALE_LOCAL_HOST=192.168.0.177
TAILSCALE_LINODE_HOST=

# ━━━ DASHBOARD ━━━
WEB_USERNAME=evin
WEB_PASSWORD=nebula2024
OPENAI_API_KEY=

# ━━━ COMMUNITY MEDIA SYNC ━━━
NODE_NAME=LocalServer
DASHBOARD_URL=https://dashboard.evindrake.net
SYNC_API_KEY=

# ━━━ DISCORD NOTIFICATIONS (Optional) ━━━
DISCORD_WEBHOOK_URL=
STORAGE_ALERT_DISCORD_WEBHOOK=

# ━━━ STORAGE MONITORING ━━━
ZFS_ENABLED=false
STORAGE_CHECK_INTERVAL=3600

# ━━━ SUNSHINE GAMESTREAM ━━━
# Sunshine runs on Windows 11 VM with QEMU/KVM + GPU passthrough
# VM uses bridged networking = gets its own IP on your LAN
# Find VM IP: run 'ip addr' inside Windows VM or check router DHCP
GAMESTREAM_HOST=192.168.0.XXX
SUNSHINE_PASS=
# For remote streaming: forward ports 47984-48010 (TCP+UDP) on router to VM

# ━━━ NAS ACCESS (Optional) ━━━
NAS_HOST=192.168.1.x
NAS_USER=admin
NAS_PASSWORD=

# ═══════════════════════════════════════════════════════════════
#  NOTES:
#  - Run ./deploy.sh to auto-generate secrets and start services
#  - Services auto-start on boot via systemd (after ./deploy.sh install)
#  - All subdomains configured in config/domains.yml
#  - Protected services require Authelia login (2FA)
# ═══════════════════════════════════════════════════════════════
